name: Build & Release

# Dispara APENAS quando o arquivo VERSION muda na main.
# Para lançar uma versão nova: mude o VERSION, faça git push.
on:
  push:
    branches: [main]
    paths:
      - 'app/VERSION'

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache-dependency-path: app/go.sum

      - name: Ler versão
        id: ver
        shell: pwsh
        run: |
          $v = (Get-Content app/VERSION).Trim()
          echo "VERSION=$v" >> $env:GITHUB_OUTPUT
          echo "Versão: $v"

      - name: Build ThazzDraco.exe
        working-directory: app
        shell: pwsh
        env:
          GOOS: windows
          GOARCH: amd64
          CGO_ENABLED: 0
        run: |
          go build -ldflags="-H windowsgui -s -w" -o ThazzDraco.exe .
          Write-Host "Build concluido: $(Get-Item ThazzDraco.exe | Select-Object -ExpandProperty Length) bytes"

      - name: Criar Release no GitHub
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $version = "${{ steps.ver.outputs.VERSION }}"
          $tag = "v$version"

          # Notas de release: commits desde a tag anterior, ou todo o log se for a 1ª
          $prevTag = git describe --tags --abbrev=0 HEAD~1 2>$null
          if ($prevTag) {
            $lines = git log "$prevTag..HEAD" --format="• %s" 2>$null
          } else {
            $lines = git log --format="• %s" -10 2>$null
          }
          $notes = if ($lines) { ($lines | Where-Object { $_ -match '\S' }) -join "`n" } else { "ThazzDraco Optimizer v$version" }

          # Remove release anterior com a mesma tag, se houver
          gh release delete $tag --yes 2>$null

          # Cria o release com o .exe
          gh release create $tag `
            --title "ThazzDraco Optimizer v$version" `
            --notes $notes `
            --latest `
            "app/ThazzDraco.exe#ThazzDraco.exe"

          Write-Host "Release $tag criado com sucesso!"
